# Note

- A key point in traversing a linked list is the terminating condition of the loop. Here, we have set the condition on `even` and `even.next` so that after finishing the loop, we can be sure that `odd` will be the tail of the odd list.
