# Note

- The main idea is to traverse the binary tree via Depth First Search (DFS) and store the leaves. If $h$ is the height of the tree, then the solution's time complexity is $\mathsf{O}(h)$ and its space complexity is $\mathsf{O}(2^h)$.
