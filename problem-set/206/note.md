# Note

- The main idea is to traverse the list with three pointers to the previous, current and next nodes and reverse the direction of links between nodes. Clearly, this solution has time complexity of $\mathsf{O}(n)$ and space complexity of $\mathsf{O}(1)$.
